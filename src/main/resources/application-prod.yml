logging:
  level:
    org.victor.stackora.mapper: WARN


server:
  servlet:
    context-path: /api
    session:
      timeout: 30m
      cookie:
        http-only: true
        same-site: lax
        secure: true
  port: 8080